# PUBLIC_INTERFACE
# Example environment variables for the React frontend.
# Copy this file to ".env" and adjust values as needed.

# Backend base URL (protocol + host + port), e.g.:
# REACT_APP_BACKEND_URL=https://vscode-internal-38494-beta.beta01.cloud.kavia.ai:3001
# If not set, the app will use relative paths and rely on the dev proxy in package.json.
REACT_APP_BACKEND_URL=
